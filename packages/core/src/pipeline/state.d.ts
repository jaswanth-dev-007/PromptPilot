import type { PipelineState } from '../types'
import type { PipelineManifest } from '../types'
export declare function detectState(
  manifest: PipelineManifest,
  outputDir: string,
): Promise<PipelineState>
//# sourceMappingURL=state.d.ts.map
