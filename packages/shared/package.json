{
  "name": "@promptpilot/shared",
  "version": "0.1.0",
  "description": "Shared utilities, error classes, and types for PromptPilot",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "clean": "rimraf dist *.tsbuildinfo"
  },
  "dependencies": {
    "pino": "^9.0.0",
    "pino-pretty": "^11.0.0",
    "gpt-tokenizer": "^2.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.0.0"
  }
}
